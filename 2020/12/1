2020/12/1 星期二 工作汇报：

1.重新整理思路，目前的想法是：
  1）车辆和RSU的注册：
  2）车辆和RSU的相互认证，并协商共享密钥。
  3）车辆使用共享密钥对身份和消息进行HMAC，发送给RSU。
  4）RSU通过HMAC来验证消息完整性，再对消息进行哈希再聚合，然后使用RSU私钥对其进行转化，发送给其通信范围内的车辆。
  5）RSU周围的车辆收到来自附近车辆的消息之后，也计算这个消息的哈希并聚合，然后通过来自RSU的转化消息以及RSU的公钥来验证收到的消息的完整性，验证通过则接收。
2.前三步跟之前设计的方案变动不大，主要是最后两步的构造，目前是在构造这一部分。